// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    webpack(config) {
      // Customize your webpack configuration if needed
      return config;
    },
  };
  
  export default nextConfig;
  